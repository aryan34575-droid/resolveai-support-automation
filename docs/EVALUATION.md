# Evaluation

`evaluate.py` measures classification accuracy, priority accuracy, routing accuracy, duplicate detection quality, groundedness, escalation rate, and processing latency after execution. `synthetic_tickets.json` is explicitly **SYNTHETIC TEST DATA** and results must not be interpreted as production performance. A meaningful evaluation requires a privacy-reviewed, human-labelled holdout set.
