name: ResolveAI analysis and dashboard

on:
  workflow_dispatch:
  issues:
    types: [opened, edited, reopened]
  schedule:
    - cron: "30 6 * * 1"

permissions:
  contents: write
  issues: read
  pages: write
  id-token: write

concurrency:
  group: resolveai-pages
  cancel-in-progress: true

jobs:
  analyze-and-publish:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      GITHUB_REPOSITORY: ${{ github.repository }}
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - run: python -m pip install -r requirements.txt
      - run: python -m compileall .
      - run: python -m pytest -q
      - name: Generate safe public report
        run: python -m src.main --github-issues --since-days 30 --public-report > docs/report.json
      - name: Validate generated report
        run: python -c "import json; r=json.load(open('docs/report.json', encoding='utf-8')); assert r['report_type']=='public_dashboard'; assert isinstance(r['issues'], list)"
      - name: Persist changed report
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/report.json
          if git diff --cached --quiet; then
            echo "Report unchanged"
          else
            git commit -m "chore: refresh ResolveAI dashboard report"
            git push
          fi
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v4
        with:
          path: docs
      - name: Deploy GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
